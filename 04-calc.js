//This is a Local Module

const add = (x, y) => {
  return x + y;
};

const sub = (x, y) => {
  return x - y;
};

const mult = (x, y) => {
  return x * y;
};

const div = (x, y) => {
  return x / y;
};

export { add, sub, mult, div };
