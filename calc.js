export const add = (x, y) => {
  return x + y;
};

export const sub = (x, y) => {
  return x - y;
};

export const mult = (x, y) => {
  return x * y;
};

export const div = (x, y) => {
  return x / y;
};
